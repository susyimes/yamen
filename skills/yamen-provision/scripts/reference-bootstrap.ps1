# Reference only.
# Use repo script as the current fallback implementation:
#   scripts/bootstrap-yamen-runtime.ps1
#
# This file exists so the skill has an explicit script anchor.
# The OpenClaw-native implementation should eventually replace this fallback.
